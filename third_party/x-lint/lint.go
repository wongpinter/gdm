// Package lint is an unused-at-runtime stub — see third_party/errgo.v2
// for why this exists. golang.org/x/lint is a linter tool pulled in
// only as a dependency-graph artifact of an old genproto release; it's
// never imported by anything we build.
package lint
