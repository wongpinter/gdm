// Package errgo is an unused-at-runtime stub: the real gopkg.in/errgo.v2
// lives behind a host this sandbox can't reach, and nothing we actually
// build imports it — it's a transitive test dependency of
// rogpeppe/go-internal pulled in only because that module predates Go's
// module graph pruning. This satisfies module resolution without pulling
// in gopkg.in.
package errgo
